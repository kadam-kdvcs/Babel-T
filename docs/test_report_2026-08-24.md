# 测试报告（2026-08-24）

> 测试对象：阿拉伯语翻译审校助手 MVP（阶段 3.2）
> 测试方式：Playwright 浏览器自动化 + Streamlit 实际页面
> 测试结果：**通过**

## 一、测试环境

| 项 | 值 |
|---|---|
| 项目路径 | `D:\G\python_coding\proj_vibe_coding\arabic-review-mvp` |
| 页面入口 | Streamlit |
| 标准端口 | 8501（真实 API/LLM 模式，读取本地 `.env`） |
| Mock 端口 | 8502（`TRANSLATION_ENGINE=mock`, `REVIEW_ENGINE=mock`） |
| 自动化工具 | Playwright MCP（浏览器交互） |
| 辅助验证 | `python -m pytest -q`：**105 passed** |

## 二、测试用例与结果

### 1. 真实 API/LLM 模式页面主链路（8501）

| 步骤 | 预期 | 结果 |
|---|---|---|
| 打开 `http://127.0.0.1:8501` | 页面标题为「阿语审校助手 MVP」，示例阿语文本已预填 | ✅ |
| 点击「开始翻译与审校」 | 显示处理中后出现结果 | ✅ |
| 翻译结果对比区 | 每段显示四个结果：原始 API 译文 / LLM 直接翻译结果 / LLM 修正结果 / LLM 最终结果 | ✅ |
| 术语命中/专名命中表 | 正常渲染 | ✅ |
| 审校报告 | 8 项结构 markdown 正常渲染 | ✅ |
| 浏览器控制台 | 无 error / warning | ✅ |

### 2. 四结果对比展示（8501）

| 步骤 | 预期 | 结果 |
|---|---|---|
| 页面加载完成后 | 每段固定展示四个结果标签 | ✅ |
| 最终结果 | 标注「LLM 最终结果（以原文为准）」 | ✅ |
| 结果切换 | 不再提供 radio；四结果始终同时展示 | ✅ |

### 3. Mock 模式主链路（8502）

| 步骤 | 预期 | 结果 |
|---|---|---|
| 打开 `http://127.0.0.1:8502` | 页面正常加载 | ✅ |
| 点击「开始翻译与审校」 | 四结果区显示「占位译文」与三个占位 LLM 结果 | ✅ |
| 术语/专名命中表 | 正常展示词库扫描结果 | ✅ |
| 审校报告 | 显示「（占位）审校报告：待接入 LLM 后生成。」 | ✅ |

### 4. 空输入校验

| 步骤 | 预期 | 结果 |
|---|---|---|
| 清空输入框并点击按钮 | 页面提示「请输入阿拉伯语文本。」，不执行流水线 | ✅ |

### 5. 并发翻译 + 动态渲染优化（2026-08-24 追加）

| 步骤 | 预期 | 结果 |
|---|---|---|
| 点击「开始翻译与审校」后立即观察 | 每段**原文立即显示**，译文位置显示「等待翻译…」 | ✅ |
| 等待约 1-2 秒 | 每段译文逐步显示在原文下方，无需等 LLM | ✅ |
| 全部段落翻译完成后 | 原文 + 译文保持显示，同时出现「全部段落翻译完成，正在请求 LLM 纠正与审校…」 | ✅ |
| LLM 返回后 | 临时占位符清空，渲染完整「翻译对照 / 术语 / 专名 / 审校报告」 | ✅ |
| 浏览器控制台 | 无 error / warning | ✅ |

### 6. 三阶段独立调用 + 四结果对比页面验证（2026-08-24 追加）

| 步骤 | 预期 | 结果 |
|---|---|---|
| 真实 API/LLM 模式点击「开始翻译与审校」 | 出现「翻译结果对比（四个结果）」 | ✅ |
| 检查每段标签 | 每段包含「原始 API 译文」「LLM 直接翻译结果」「LLM 修正结果」「LLM 最终结果（以原文为准）」 | ✅ |
| 检查翻译取舍说明 | 页面出现「翻译取舍说明」区 | ✅ |
| 浏览器控制台 | 无 error / warning | ✅ |

## 三、发现并修复的问题

| 问题 | 严重度 | 处理 |
|---|---|---|
| 页面顶部说明仍写「第 3 阶段」，且未提到 LLM 多轮结果 | 低（文案一致性） | 已更新为「第 3.2 阶段」，并说明 LLM 输出直接翻译/修正/最终结果 |
| 功能逻辑问题 | — | Playwright 主链路未发现新的功能性 bug；已有单元测试全绿 |

## 四、说明

- 本次 Playwright 测试覆盖了真实 API/LLM 模式、Mock 模式、四结果对比、空输入校验。
- 缺少 API 密钥的“回退占位 + 黄色警告”场景由 `tests/test_reviewer.py` 和 `tests/test_translator.py` 覆盖；浏览器侧因环境变量包含真实密钥，未强制触发。
- 后续建议：完成 Django + TS 前端后，在同一测试报告模板上补充新前端 UI 测试。
